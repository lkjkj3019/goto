<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.Hotel.mapper.H_InfoMapper">

<!-- 호텔 정보 select -->
<select id="bookingCheck" parameterType="String" resultType="com.Hotel.dto.H_InfoDTO">
	SELECT * FROM H_INFO WHERE IN_HOCODE = #{hocode}
</select>

</mapper>